"use strict";

var CONTROLLER = {
    main: function (use) {
        let ajax = use.ajax;
        let render = use.render;
        let session = use.session;
        let notification = use.extension.notification;


        __construct.call (this);

        function __construct () {

        }
    }
};
